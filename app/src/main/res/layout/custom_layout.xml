<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <ImageView
        android:id="@+id/letterImageView"
        android:layout_width="wrap_content"
        android:layout_height="100dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="28dp"
        android:layout_marginStart="28dp"
        app:srcCompat="@drawable/a" />

    <TextView
        android:id="@+id/letterTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/letterImageView"
        android:layout_alignStart="@+id/letterImageView"
        android:layout_below="@+id/letterImageView"
        android:textSize="30dp"
        android:text="A"
        android:textAlignment="center"
        android:layout_alignRight="@+id/letterImageView"
        android:layout_alignEnd="@+id/letterImageView" />
</RelativeLayout>